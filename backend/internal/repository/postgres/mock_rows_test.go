package postgres

import "strings"

var stepMockColumns = strings.Split(stepColumns, ", ")
var buildMockColumns = strings.Split(buildColumns, ", ")
var executionJobMockColumns = strings.Split(executionJobColumns, ", ")
