services:
  migrate:
    image: golang:${GO_VERSION}
    container_name: coyote-ci-migrate
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      DATABASE_URL: ${DATABASE_URL}
      GOOSE_MIGRATION_DIR: /migrations
    volumes:
      - ./backend/db/migrations:/migrations:ro
    entrypoint: ["/bin/sh", "-ec"]
    command:
      - 'go run github.com/pressly/goose/v3/cmd/goose@v3.24.1 -dir "$${GOOSE_MIGRATION_DIR}" postgres "$${DATABASE_URL}" up'
    restart: "no"

  server:
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        GO_VERSION: ${GO_VERSION}
    container_name: coyote-ci-server
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      migrate:
        condition: service_completed_successfully
    environment:
      APP_PORT: "8080"
      ARTIFACT_STORAGE_ROOT: /var/lib/coyote-artifacts

      DATABASE_URL: ${DATABASE_URL}
      GITHUB_WEBHOOK_SECRET: ${GITHUB_WEBHOOK_SECRET}
      AUTH_MODE: ${AUTH_MODE:-disabled}
      BOOTSTRAP_ADMIN_EMAILS: ${BOOTSTRAP_ADMIN_EMAILS:-}
      OIDC_ISSUER_URL: ${OIDC_ISSUER_URL:-}
      OIDC_CLIENT_ID: ${OIDC_CLIENT_ID:-}
      OIDC_CLIENT_SECRET: ${OIDC_CLIENT_SECRET:-}
      OIDC_REDIRECT_URL: ${OIDC_REDIRECT_URL:-}
      OIDC_SCOPES: ${OIDC_SCOPES:-openid email profile}
      SESSION_SECRET: ${SESSION_SECRET:-}
      SESSION_COOKIE_NAME: ${SESSION_COOKIE_NAME:-coyote_session}
      SESSION_COOKIE_SECURE: ${SESSION_COOKIE_SECURE:-true}
      SESSION_COOKIE_SAME_SITE: ${SESSION_COOKIE_SAME_SITE:-lax}
      COYOTE_PUBLIC_URL: ${COYOTE_PUBLIC_URL:-}
      COYOTE_WORKSPACE_HELPER_ENABLED: ${COYOTE_WORKSPACE_HELPER_ENABLED:-false}
      COYOTE_WORKSPACE_HELPER_KUBECONFIG: /etc/coyote/gke-verifier-kubeconfig
      COYOTE_WORKSPACE_HELPER_SERVICE_ACCOUNT: ${COYOTE_WORKSPACE_HELPER_SERVICE_ACCOUNT:-coyote-workspace-helper}
      COYOTE_WORKSPACE_HELPER_CAPABILITY_SECRET: ${COYOTE_WORKSPACE_HELPER_CAPABILITY_SECRET:-}
      COYOTE_WORKSPACE_REVISION_STORAGE_ROOT: /var/lib/coyote-workspaces
      COYOTE_KUBERNETES_CACHE_HELPER_ENABLED: "true"
      WORKER_CACHE_STORAGE_PROVIDER: gcs
      WORKER_CACHE_STORAGE_STRICT: "true"
      WORKER_CACHE_GCS_BUCKET: ${WORKER_CACHE_GCS_BUCKET:?WORKER_CACHE_GCS_BUCKET is required for Kubernetes cache helpers}
      WORKER_CACHE_GCS_PREFIX: ${WORKER_CACHE_GCS_PREFIX:-coyote-ci/cache}
      WORKER_CACHE_GCS_PROJECT: ${WORKER_CACHE_GCS_PROJECT:-}

      ARTIFACT_STORAGE_PROVIDER: ${ARTIFACT_STORAGE_PROVIDER}
      ARTIFACT_GCS_BUCKET: ${ARTIFACT_GCS_BUCKET}
      ARTIFACT_GCS_PREFIX: ${ARTIFACT_GCS_PREFIX}
      ARTIFACT_GCS_PROJECT: ${ARTIFACT_GCS_PROJECT}
    ports:
      - "8080:8080"
    volumes:
      - artifact_data:/var/lib/coyote-artifacts
      - workspace_revision_data:/var/lib/coyote-workspaces
      - ${COYOTE_WORKSPACE_HELPER_KUBECONFIG_HOST_PATH:-./secrets/gke-verifier-kubeconfig}:/etc/coyote/gke-verifier-kubeconfig:ro

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: coyote-ci-frontend
    depends_on:
      - server
    ports:
      - "3000:3000"

volumes:
  artifact_data:
  workspace_revision_data:
